{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build-docker",
            "command": "docker build --tag mp_tick_data .",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
        }
    ]
}
